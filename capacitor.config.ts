import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.inta.criolloatm',
  appName: 'Criollo Atomizadores',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
